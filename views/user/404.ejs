<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* --- Shared Dark Theme Variables --- */
        :root {
            --primary-color: #6366f1;
            --primary-hover: #4f46e5;
            --bg-color: #0f172a;      /* Slate-900 */
            --card-bg: #1e293b;       /* Slate-800 */
            --text-main: #f8fafc;     /* Slate-50 */
            --text-muted: #94a3b8;    /* Slate-400 */
            --border-color: #334155;  /* Slate-700 */
        }

        body {
            background-color: var(--bg-color);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            font-family: 'Segoe UI', system-ui, sans-serif;
            color: var(--text-main);
        }

        .container {
            text-align: center;
            position: relative;
            z-index: 10;
        }

        /* The Big 404 Text - Updated to Indigo Gradient */
        .text-huge {
            font-size: 10rem;
            font-weight: 900;
            line-height: 1;
            /* Gradient from Primary Indigo to a lighter shade */
            background: linear-gradient(135deg, var(--primary-color), #a5b4fc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            position: relative;
            animation: floatText 3s ease-in-out infinite;
            text-shadow: 0 10px 30px rgba(99, 102, 241, 0.3);
        }

        .sub-text {
            font-size: 1.25rem;
            color: var(--text-muted);
            margin-bottom: 40px;
        }

        /* --- Modern Button Styles (Matching Login/Home) --- */
        .btn-modern {
            padding: 12px 35px;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
        }

        /* Primary Button */
        .btn-primary-modern {
            background-color: var(--primary-color);
            color: white;
            border: 2px solid var(--primary-color);
        }
        
        .btn-primary-modern:hover {
            background-color: var(--primary-hover);
            border-color: var(--primary-hover);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(99, 102, 241, 0.4);
        }

        /* Outline Button (Ghost) */
        .btn-outline-modern {
            background-color: transparent;
            border: 2px solid var(--border-color);
            color: var(--text-muted);
        }
        
        .btn-outline-modern:hover {
            border-color: var(--text-main);
            color: var(--text-main);
            background: rgba(255, 255, 255, 0.05);
        }

        /* Subtle background glow blob */
        .bg-circle {
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, rgba(15, 23, 42, 0) 70%);
            width: 600px;
            height: 600px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: -1;
            pointer-events: none;
        }

        @keyframes floatText {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }
    </style>
</head>
<body>

    <div class="bg-circle"></div>

    <div class="container">
        <div class="text-huge">404</div>
        <h2 class="fw-bold mb-3">Page Not Found</h2>
        <p class="sub-text">We can't seem to find the page you're looking for.</p>
        
        <div class="d-flex justify-content-center gap-3">
            <a href="home.html" class="btn btn-modern btn-primary-modern">Back to Home</a>
            <a href="#" onclick="history.back()" class="btn btn-modern btn-outline-modern">Go Back</a>
        </div>
    </div>

</body>
</html>