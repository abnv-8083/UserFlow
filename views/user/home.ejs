<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Dark Mode</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* --- Shared Dark Theme Variables --- */
        :root {
            --primary-color: #6366f1;
            --primary-hover: #4f46e5;
            --bg-color: #0f172a;      /* Slate-900 */
            --card-bg: #1e293b;       /* Slate-800 */
            --text-main: #f8fafc;     /* Slate-50 */
            --text-muted: #94a3b8;    /* Slate-400 */
            --border-color: #334155;  /* Slate-700 */
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'Segoe UI', system-ui, sans-serif;
            overflow-x: hidden; /* Prevent scrollbar during animation */
        }

        /* --- Navbar Styling --- */
        .navbar {
            background-color: var(--card-bg) !important;
            border-bottom: 1px solid var(--border-color);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: 700;
            color: var(--text-main) !important;
            letter-spacing: -0.5px;
        }

        .text-muted-custom {
            color: var(--text-muted) !important;
        }

        /* --- Hero Section with Animation --- */
        .hero-section {
            /* Deep Indigo Gradient to match theme */
            background: linear-gradient(135deg, #1e293b 0%, #312e81 100%);
            padding: 80px 0 100px;
            border-radius: 0 0 50px 50px;
            color: white;
            position: relative;
            border-bottom: 1px solid var(--border-color);
            /* Animation */
            animation: fadeInDown 0.8s ease-out;
        }

        .btn-glow {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 35px;
            border-radius: 30px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 0 15px rgba(99, 102, 241, 0.5);
        }

        .btn-glow:hover {
            background: var(--primary-hover);
            transform: translateY(-3px);
            box-shadow: 0 0 25px rgba(99, 102, 241, 0.7);
            color: white;
        }

        /* --- Dashboard Cards --- */
        .feature-card {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 30px;
            height: 100%;
            transition: all 0.3s ease;
            /* Hidden initially for animation */
            opacity: 0; 
            transform: translateY(20px);
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .feature-card h5 {
            color: var(--text-main);
            font-weight: 600;
            margin-bottom: 15px;
        }

        .feature-card p {
            color: var(--text-muted);
        }

        /* Hover Effect */
        .feature-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary-color);
            box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.5);
        }

        /* --- Animations --- */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Staggered Delays for Cards */
        .delay-1 { animation-delay: 0.2s; }
        .delay-2 { animation-delay: 0.3s; }
        .delay-3 { animation-delay: 0.4s; }

    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg py-3 sticky-top">
    <div class="container">
        <a class="navbar-brand" href="#">ModernApp</a>
        <div class="d-flex align-items-center">
            <span class="me-3 text-muted-custom">Hello, <%= name %></span>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill px-4">Logout</a>
        </div>
    </div>
</nav>

<div class="hero-section text-center">
    <div class="container">
        <h1 class="display-4 fw-bold mb-3">Welcome to Your Dashboard</h1>
        <p class="lead mb-4 opacity-75" style="color: var(--text-muted);">Secure, fast, and beautifully designed for dark mode.</p>
        <button class="btn btn-glow">Get Started</button>
    </div>
</div>

<div class="container mt-n5" style="margin-top: -50px;"> <div class="row g-4">
        
        <div class="col-md-4">
            <div class="feature-card delay-1">
                <h5>Profile</h5>
                <p>Manage your personal details, avatar, and account security settings from one place.</p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="feature-card delay-2">
                <h5>Settings</h5>
                <p>Configure your application preferences, notifications, and theme settings easily.</p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="feature-card delay-3">
                <h5>Support</h5>
                <p>Need help? Contact our support team 24/7 or browse our documentation.</p>
            </div>
        </div>

    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if (typeof msg !== 'undefined') { %>
        <script>
            Swal.fire({
                title: "<%=msg%>",
                text: "That thing is still around?",
                icon: "<%=icon%>",
                background:'#0F172A',
                color: '#DADACE',
            });
            const url = window.location.protocol + "//" + window.location.host + window.location.pathname;
            window.history.replaceState({path: url}, '', url);
        </script>
    <% } %>

</body>
</html>